package com.vibeshelf.vibeshelf_backend.service;

/**
 * Empty placeholder BookService. The original enrichment service was removed because
 * the canonical table no longer stores the external IDs used for enrichment. Keep
 * a minimal class here to avoid compilation issues while callers are migrated.
 */
public class BookService {
    // intentionally empty - enrichment removed
}
